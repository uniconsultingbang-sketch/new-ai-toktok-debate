# Demo Stabilization QA - 2026-05-10

대상: 내일 사장님 시연용 안정화

## 기준점

- 백업: `C:\Users\unico\Documents\Codex\2026-05-07\backups\demo-stabilization-source-20260510-224137.zip`
- Git baseline: `baseline-before-demo-stabilization`
- 작업 브랜치: `codex/demo-stabilization`
- 원칙: 기능 추가, 큰 디자인 변경, 불필요한 리팩토링 없이 시연 흐름 안정화만 확인

## 로컬 실제 화면 QA

검수 환경: `http://localhost:3000`

| 시나리오 | 결과 | 메모 |
| --- | --- | --- |
| 홈 진입 | 통과 | 200 응답 및 홈 화면 표시 |
| 빈 질문 제출 | 통과 | `질문을 한 줄만 넣어 주세요.` 표시 |
| 기본값 심플 모드 | 통과 | 홈 진입 시 심플 선택 |
| 질문 입력 후 토론 시작 | 통과 | 결과 화면 `/decisions/:id` 이동 |
| 결과 화면 STEP/Live Debate | 통과 | STEP 1~4 및 Live Debate 표시 |
| 빠르게 버튼 | 통과 | 최종 리포트까지 완료 |
| 홈 복귀 | 통과 | 결과 상단 홈 컨트롤로 `/` 복귀 |
| 최근 토론 표시 | 통과 | 완료/중단 상태와 질문 표시 |
| 최근 토론 다시 보기 | 통과 | 최근 항목 클릭 시 동일 결과 URL 재진입 |
| 완료 기록 새 로직 재실행 | 통과 | `새 로직으로 다시 토론` 표시 및 재생성 |
| 중단 버튼 | 통과 | 홈 복귀, 최근 목록 `중단됨` 표시 |
| 중단 기록 재진입 | 통과 | 자동 재시작하지 않고 `처음부터 다시 토론` 표시 |
| 미완료 running 복구 | 통과 | 새로고침 후 자동 복구 및 완료 |
| 심층 모드 | 통과 | 추가 필드 표시, 결과 완료, 홈 복귀, 최근 표시 |

## 테스트 질문 결과

| 질문 | 결과 |
| --- | --- |
| `AI와 어떻게 말해야 해?` | 통과 |
| `AI와 어떻게 알해야 해?` | 통과 |
| `AI에게 일을 잘 시키려면?` | 통과 |
| `오늘 회식은 짜장면이 좋을까?` | 통과 |
| `신입과 경력직 중 누구를 뽑아야 할까?` | 통과 |
| `AI는 사람의 감정을 신경 쓰나?` | 통과 |
| `졸리지 않는 감기약 개발이 필요한가?` | 통과 |

## 수정 및 확인 사항

- 상단 홈 컨트롤은 실제 링크와 강제 홈 이동 fallback을 함께 사용한다.
- 시작 버튼은 중복 클릭 방지와 진행 문구를 제공한다.
- 완료 기록도 새 로직으로 다시 생성할 수 있다.
- `질문라면` 같은 어색한 fallback 조사는 `“질문”이라면` 형태로 보정했다.
- 개발 서버 실행 중 production build를 섞지 않고, 서버 중지 후 `.next` 정리 상태에서 build를 수행했다.

## 정적 검수

- TypeScript: 통과
- Production build: 통과

## 배포 URL 검수 상태

- 공식 URL: `https://ai-3-mind-council.vercel.app`
- 상태: 미통과 / Critical
- 증상: 공식 URL이 현재 로컬 최종본이 아니라 예전 `AI Decision Council` 화면을 표시한다.
- 원인: Vercel production이 GitHub `main`의 예전 소스를 기준으로 배포되어 있고, 현재 안정화 브랜치의 로컬 수정이 production에 반영되지 않았다.
- 추가 확인: Vercel CLI 직접 배포는 현재 PC 세션의 로그인/경로 문제로 진행되지 않았다.
- 다음 조치: Vercel 로그인 또는 GitHub production 반영 경로가 확보되면 production 배포 후 동일 시나리오를 배포 URL에서 재검수한다.
- 배포 URL 검수 완료 전에는 `qa-pass-demo-flow` 커밋을 만들지 않는다.
